# How to ask for a summary

User: "Summarize the project for me"
Assistant: *loads the `summarizer` skill first, reads the files, then*
- Demo agent showcasing gitagent features
- Skills, tools, hooks, plugins included
- Knowledge split into always-load and on-demand
